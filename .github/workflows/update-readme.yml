name: Auto Update README

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Generate README.md
        run: |
          echo "# 🎓 สมิตา ศรีแก้ว – Resume Website" > README.md
          echo "" >> README.md
          echo "🔗 **Live Preview:** [pongpangsamita.github.io/resume](https://pongpangsamita.github.io/resume)" >> README.md
          echo "" >> README.md
          echo "🕒 Updated on: $(date '+%Y-%m-%d %H:%M:%S')" >> README.md
          echo "" >> README.md

      - name: Commit & Push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "Auto update README on $(date '+%Y-%m-%d %H:%M:%S')" || echo "No changes to commit"
          git push
